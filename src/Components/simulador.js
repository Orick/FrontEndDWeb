import React, { Component } from 'react';

class Simulador extends Component {
    render() {
        return (
            <div>
                Simulador
            </div>
        );
    }
}

export default Simulador;